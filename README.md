# AI Engineer Resource Registry Completion

Only two project files need to be replaced:

- `content/references/reference-catalog.json`
- `scripts/validate-references.mjs`

The existing AI Engineer career data and UI components must not be replaced.

Run:

```powershell
node scripts/validate-references.mjs
npm run typecheck
npm run build
```
