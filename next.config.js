// next.config.js
// Use .js — next.config.ts is only supported in Next.js 15.1+ with specific setups.
// Keeping this as plain JS avoids version compatibility issues.

/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special configuration needed for the landing page MVP.
  // Add image domains, redirects, or env vars here as the project grows.
};

module.exports = nextConfig;
