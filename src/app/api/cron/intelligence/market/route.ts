import { NextResponse } from "next/server"; import { authorized } from "@/lib/intelligence/refreshAuth"; import { runRefresh } from "@/lib/intelligence/refreshEngine"; import { scheduledCountries } from "@/lib/intelligence/snapshotRegistry";
export async function GET(request:Request){if(!authorized(request))return NextResponse.json({error:"Unauthorized"},{status:401});try{return NextResponse.json(await runRefresh({careerSlug:"ai-engineer",countries:scheduledCountries().map(item=>item.productCode),type:"market",trigger:"cron"}))}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Refresh failed"},{status:503})}}
