import Link from "next/link";
import { notFound } from "next/navigation";
import CareerForm from "@/components/admin/CareerForm";
import ArchiveCareerControl from "@/components/admin/ArchiveCareerControl";
import { publishCareerAction, unpublishCareerAction, updateCareerAction } from "@/app/admin/(studio)/careers/actions";
import { requireAdmin } from "@/lib/admin/adminAuth";
import { getCareer, listAudit } from "@/lib/admin/careerRepository";
export default async function EditCareerPage({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{saved?:string;error?:string}>}){const authorization=await requireAdmin();if(authorization.status!=="admin")return null;const {id}=await params,[career,audit]=await Promise.all([getCareer(authorization.accessToken,id),listAudit(authorization.accessToken,id,8)]);if(!career)notFound();const query=await searchParams;const ready=Boolean(career.workspace_data)&&!(career.validation_errors?.length);return <main className="p-4 sm:p-8"><Link href="/admin/careers" className="text-sm text-cyan-300 underline">← Managed careers</Link><div className="mt-5 flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Career Management</p><h2 className="mt-2 font-display text-3xl font-semibold text-white">{career.title}</h2><p className="mt-2 text-sm text-slate-400">Status: <span className="font-semibold uppercase text-slate-200">{career.status}</span> · Content v{career.content_version} · Updated {new Date(career.updated_at).toLocaleString()}</p></div><ArchiveCareerControl id={career.id} archived={career.status==="archived"}/></div>{query.saved?<p role="status" className="mt-5 rounded-xl bg-emerald-400/10 p-3 text-sm text-emerald-200">Career {query.saved} successfully.</p>:null}{query.error?<p role="alert" className="mt-5 rounded-xl bg-rose-400/10 p-3 text-sm text-rose-200">The requested state change could not be completed.</p>:null}<section className="mt-5 flex flex-wrap items-center gap-3 rounded-xl border border-cyan-300/15 bg-cyan-400/5 p-4"><Link href={`/admin/careers/${id}/content`} className="btn-primary min-h-11">Open Content Studio</Link>{career.workspace_data?<Link href={`/admin/careers/${id}/learning`} className="btn-secondary min-h-11">Learning CMS</Link>:null}{career.workspace_data?<Link href={`/admin/careers/${id}/assessments`} className="btn-secondary min-h-11">Assessment Studio</Link>:null}{career.workspace_data?<Link href={`/admin/careers/${id}/projects`} className="btn-secondary min-h-11">Projects & Portfolio</Link>:null}{career.workspace_data?<Link href={`/admin/careers/${id}/resources`} className="btn-secondary min-h-11">AI Resource Finder</Link>:null}{career.workspace_data?<Link href={`/admin/careers/${id}/preview`} className="btn-secondary min-h-11">Preview</Link>:null}{career.status==="published"?<form action={unpublishCareerAction}><input type="hidden" name="id" value={id}/><button className="btn-secondary min-h-11">Unpublish</button></form>:<form action={publishCareerAction}><input type="hidden" name="id" value={id}/><button disabled={!ready||career.status==="archived"} className="btn-secondary min-h-11 disabled:cursor-not-allowed disabled:opacity-40">Publish</button></form>}<p className="w-full text-xs text-slate-400">{ready?`Validated and ready. Public URL: /careers/${career.slug}`:"Complete and validate workspace content before publishing."}</p></section><div className="mt-7 max-w-5xl"><CareerForm action={updateCareerAction} career={career}/></div><section className="mt-8 max-w-5xl rounded-2xl border border-white/10 bg-white/[.025] p-5"><h3 className="font-semibold text-white">Recent audit activity</h3>{audit.length?<ul className="mt-3 divide-y divide-white/10">{audit.map(entry=><li key={entry.id} className="flex flex-wrap justify-between gap-2 py-3 text-sm"><span>{entry.action.replace("career.","Career ")} <span className="text-slate-500">· {(entry.changed_fields.fields??[]).join(", ")}</span></span><time className="text-slate-500">{new Date(entry.created_at).toLocaleString()}</time></li>)}</ul>:<p className="mt-3 text-sm text-slate-500">No audit activity is available.</p>}</section></main>}
