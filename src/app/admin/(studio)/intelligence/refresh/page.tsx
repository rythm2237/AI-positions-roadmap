import Link from"next/link";import{RefreshForm}from"@/components/admin/IntelligenceControls";import{requireAdmin}from"@/lib/admin/adminAuth";import{listCareers}from"@/lib/admin/careerRepository";import codeCareers from"../../../../../../content/intelligence/career-query-registry.json" with{type:"json"};
export default async function RefreshPage(){const auth=await requireAdmin();if(auth.status!=="admin")return null;const managed=await listCareers(auth.accessToken,{status:"draft"}),careers=[...codeCareers.filter(c=>c.enabled).map(c=>({slug:c.careerSlug,title:c.canonicalTitle,complete:Boolean(c.canonicalTitle&&c.directTitles.length)})),...managed.map(c=>({slug:c.slug,title:c.title,complete:Boolean(c.taxonomy.primaryTitle)}))];return <main className="p-4 sm:p-8"><Link href="/admin/intelligence" className="text-sm text-cyan-300 underline">← Intelligence Data</Link><h2 className="mt-5 font-display text-3xl font-semibold text-white">Start bounded Adzuna refresh</h2><p className="mt-2 max-w-3xl text-sm text-slate-400">This queues persisted work. Each processing request handles one Career/country/type item and creates a candidate for review; published data remains unchanged.</p><div className="mt-7 max-w-3xl rounded-2xl border border-white/10 bg-white/[.025] p-5"><RefreshForm careers={careers}/></div></main>}
