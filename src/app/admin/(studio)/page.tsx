import Link from "next/link";
import { requireAdmin } from "@/lib/admin/adminAuth";
import { listAudit, listCareers } from "@/lib/admin/careerRepository";
export default async function AdminDashboard(){const authorization=await requireAdmin();if(authorization.status!=="admin")return null;const [careers,audit]=await Promise.all([listCareers(authorization.accessToken),listAudit(authorization.accessToken,undefined,6)]);const drafts=careers.filter(item=>item.status==="draft").length,archived=careers.filter(item=>item.status==="archived").length;return <main className="p-4 sm:p-8"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Operations overview</p><h2 className="mt-2 font-display text-3xl font-semibold text-white">Admin dashboard</h2><p className="mt-2 text-sm text-slate-400">Database-managed drafts are separate from existing code-driven public Career pages.</p></div><Link href="/admin/careers/new" className="btn-primary min-h-11">Create Career</Link></div><div className="mt-7 grid gap-3 sm:grid-cols-3"><Metric label="Managed careers" value={careers.length}/><Metric label="Draft careers" value={drafts}/><Metric label="Archived careers" value={archived}/></div><section className="mt-8 rounded-2xl border border-white/10 bg-white/[.025] p-5"><h3 className="font-semibold text-white">Recent Admin activity</h3>{audit.length?<ul className="mt-3 divide-y divide-white/10">{audit.map(entry=><li key={entry.id} className="flex flex-wrap justify-between gap-2 py-3 text-sm"><span className="text-slate-300">{entry.action.replace("career.","Career ")}</span><time className="text-slate-500">{new Date(entry.created_at).toLocaleString()}</time></li>)}</ul>:<p className="mt-3 text-sm text-slate-500">No Admin activity has been recorded yet.</p>}</section></main>}
function Metric({label,value}:{label:string;value:number}){return <div className="rounded-2xl border border-white/10 bg-white/[.025] p-5"><p className="text-xs text-slate-500">{label}</p><p className="mt-2 text-3xl font-semibold text-white">{value}</p></div>}
