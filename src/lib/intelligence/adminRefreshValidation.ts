import providerCountries from "../../../content/intelligence/country-registry.json" with{type:"json"};
import type{RefreshCareerDefinition}from"./refreshEngine.ts";
export const MAX_ADMIN_REFRESH_COUNTRIES=5,MAX_ADMIN_SAMPLE_SIZE=200,DEFAULT_ADMIN_SAMPLE_SIZE=100,REFRESH_COOLDOWN_MINUTES=30;
const supported=new Set(providerCountries.filter(country=>country.configured).map(country=>country.productCode));
export function validateAdminRefresh(input:{careerSlug:string;countries:string[];types:string[];provider:string;sampleSize:number;definition?:RefreshCareerDefinition|null}){const errors:string[]=[];if(!input.definition||!input.definition.canonicalTitle||!input.definition.directTitles.length)errors.push("INCOMPLETE_CAREER_TAXONOMY");if(input.provider!=="adzuna")errors.push("INVALID_PROVIDER");if(!input.countries.length||input.countries.length>MAX_ADMIN_REFRESH_COUNTRIES)errors.push("INVALID_COUNTRY_COUNT");if(input.countries.some(code=>!supported.has(code)))errors.push("UNSUPPORTED_COUNTRY");if(!input.types.length||input.types.some(type=>type!=="market"&&type!=="salary"))errors.push("INVALID_SNAPSHOT_TYPE");if(!Number.isInteger(input.sampleSize)||input.sampleSize<1||input.sampleSize>MAX_ADMIN_SAMPLE_SIZE)errors.push("INVALID_SAMPLE_SIZE");return{valid:!errors.length,errors}}
