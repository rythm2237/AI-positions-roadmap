import type { CareerIntelligenceResult, IntelligenceFilters } from "@/types/careerIntelligence";
import type { IntelligenceSourceAdapter } from "./adapters/types";

export const defaultIntelligenceFilters: IntelligenceFilters = {country:"",region:"",city:"",experienceLevel:"All levels",seniority:"All levels",employmentType:"All",workModel:"All",industry:"All",timePeriod:"12 months",currency:"USD",salaryType:"base",compareCountries:[]};
export function emptyIntelligenceResult(careerId:string):CareerIntelligenceResult{return {market:{careerId,geography:"No geography selected",seniority:"All levels",timePeriod:"12 months",availability:"not-configured",evidence:{sourceIds:["jobs.provider.unconfigured"],coverage:"No connected labor-market source",methodology:"No calculations are performed until a permitted source is configured.",reliability:"unverified",warnings:["Source not configured"]}},salaries:[],skills:[],mobility:[],forecast:{availability:"insufficient-data",methodology:"A forecast requires comparable historical snapshots and stable source coverage.",uncertainty:"Insufficient data for a reliable forecast",sourceIds:[]},warnings:["Live labor-market and salary sources are not configured."]};}
export class CareerIntelligenceService { constructor(private adapters:IntelligenceSourceAdapter[]){ } async getCareerIntelligence(careerId:string,filters:IntelligenceFilters,signal?:AbortSignal){const configured=this.adapters.filter((adapter)=>adapter.isConfigured());if(!configured.length)return emptyIntelligenceResult(careerId);const results=await Promise.all(configured.map((adapter)=>adapter.getCareerIntelligence(careerId,filters,signal)));return results[0]??emptyIntelligenceResult(careerId);} }
