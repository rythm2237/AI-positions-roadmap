export type IntelligenceSourceStatus = "active" | "needs-review" | "broken" | "deprecated" | "not-configured";
export interface IntelligenceSource { sourceId:string; sourceName:string; sourceType:string; provider:string; jurisdiction:string; country?:string; canonicalUrl:string|null; apiEndpoint?:string|null; dataLicense:string; authenticationRequirement:string; updateFrequency:string; coverage:string; capabilities?:Record<string,string>; supportedMarkets?:string[]; effectiveFrom?:string; effectiveTo?:string; lastVerifiedAt:string; nextReviewAt:string; status:IntelligenceSourceStatus; methodologyNotes:string; reliabilityTier:"official"|"high"|"moderate"|"low"|"unverified" }
